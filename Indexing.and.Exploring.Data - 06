#Kemuel Hepburn
#Indexing and Exploring Data

#Load a dataset of your choice (pd.read_csv() or sns.load_dataset())
#Print the first and last 5 rows.
#Show .info() and .describe() results.
#Print the column names. Rename one of them.
#Bonus: What percentage of rows have any missing values?

## Load a dataset
tips = sns.load_dataset("tips")

# First + last 5 rows
print("First 5 rows:")
print(tips.head())

print("\nLast 5 rows:")
print(tips.tail())

# Info + describe
print("\nInfo:")
tips.info()

print("\nDescribe:")
print(tips.describe(include="all"))

# Column names
print("\nColumns:", tips.columns.tolist())

# Rename a column
tips.rename(columns={"sex": "customer_gender"}, inplace=True)
print("\nAfter renaming:")
print(tips.head())

# Bonus: Percent of rows with *any* missing values
percent_missing = tips.isnull().any(axis=1).mean() * 100
print(f"\nPercent of rows with missing values: {percent_missing:.2f}%")
